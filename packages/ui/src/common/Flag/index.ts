export {
  Flag,
  FLAG_SIZES,
  FLAG_SVG_BY_CODE,
  FLAG_CODES,
  flagVariants,
  getFlagSvg,
  isFlagCode,
  normalizeFlagCode,
  resolveFlagSize,
} from "./Flag";
export type { FlagCode, FlagProps, FlagSize, FlagSizeToken } from "./Flag.types";
