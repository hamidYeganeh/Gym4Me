# Legacy marketing assets (quarantined)

`styles/` and `scripts/` are temporary contracts for the GSAP / scroll marketing home.

Rules:
- Do not add new product UI here.
- New sections use Tailwind + HeroUI under `src/modules/marketing`.
- Prefer deleting these assets once scroll modules no longer need BEM/`data-*` hooks.
