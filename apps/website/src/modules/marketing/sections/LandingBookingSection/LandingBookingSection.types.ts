export type LandingBookingSectionProps = {
  className?: string;
};

export type LandingBookingOrbitAvatar = {
  id: number;
  size: number;
  top: string;
  left: string;
  src: string;
  alt: string;
  fallback: string;
};

export type LandingBookingStackAvatar = {
  id: number;
  src: string;
  alt: string;
  fallback: string;
};
