import { Suspense } from "react";
import { BookingServiceScreen } from "@/modules/athlete/screens/BookingCheckoutFlow";
export default function Page() { return <Suspense><BookingServiceScreen /></Suspense>; }
