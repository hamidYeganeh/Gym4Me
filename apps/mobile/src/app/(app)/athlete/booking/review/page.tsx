import { Suspense } from "react";
import { BookingReviewScreen } from "@/modules/athlete/screens/BookingCheckoutFlow";
export default function Page() { return <Suspense><BookingReviewScreen /></Suspense>; }
