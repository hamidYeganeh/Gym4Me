import { Suspense } from "react";
import { BookingTimeScreen } from "@/modules/athlete/screens/BookingCheckoutFlow";
export default function Page() { return <Suspense><BookingTimeScreen /></Suspense>; }
