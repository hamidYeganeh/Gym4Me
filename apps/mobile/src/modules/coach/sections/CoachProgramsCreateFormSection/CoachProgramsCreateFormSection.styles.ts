import { tv } from "tailwind-variants";

export const coachProgramsCreateFormSectionVariants = tv({
  slots: {
    root: "flex flex-col gap-3 rounded-[24px] border-0 bg-surface p-4",
    actions: "mt-1 flex gap-2",
  },
});
