import { tv } from "tailwind-variants";

export const coachClientDetailTrendSectionVariants = tv({
  slots: {
    root: "flex flex-col gap-3",
    title: "text-foreground",
    chartCard: "rounded-[24px] border-0 bg-surface p-4",
  },
});
