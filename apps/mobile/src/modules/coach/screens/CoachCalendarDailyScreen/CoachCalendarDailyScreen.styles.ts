export const coachCalendarDailyScreenStyles = {
  root: "bg-background",
  content: "flex flex-col gap-4 pt-1",
  fab: "fixed end-screen bottom-[max(1.5rem,env(safe-area-inset-bottom))] z-20",
} as const;
