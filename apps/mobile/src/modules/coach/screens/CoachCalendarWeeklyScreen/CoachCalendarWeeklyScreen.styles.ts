export const coachCalendarWeeklyScreenStyles = {
  root: "bg-background",
  content: "flex flex-col pt-1",
  fab: "fixed end-screen bottom-[max(1.5rem,env(safe-area-inset-bottom))] z-20",
} as const;
