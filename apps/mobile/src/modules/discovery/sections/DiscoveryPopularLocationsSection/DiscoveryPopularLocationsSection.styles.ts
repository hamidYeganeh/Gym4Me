import { tv } from "tailwind-variants";

export const discoveryPopularLocationsSectionVariants = tv({
  slots: {
    grid: "grid w-full min-w-0 grid-cols-2 gap-x-4 gap-y-5",
    item: "min-w-0",
  },
});
