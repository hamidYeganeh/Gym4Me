export const discoveryMapCoachSectionStyles = {
  root: "pointer-events-none absolute inset-x-0 bottom-0 z-30 px-screen pb-[max(1.25rem,env(safe-area-inset-bottom))]",
  card: "pointer-events-auto",
} as const;
