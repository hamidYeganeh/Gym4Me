import { tv } from "tailwind-variants";

export const discoveryHomeSportsSectionVariants = tv({
  slots: {
    slide: "!w-[200px]",
    card: "!h-[270px] !w-full border-0",
  },
});

export { HOME_SPORT_THEMES } from "../../lib/sports-home";
