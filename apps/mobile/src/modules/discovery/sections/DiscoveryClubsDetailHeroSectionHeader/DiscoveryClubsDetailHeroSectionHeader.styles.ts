export const discoveryClubsDetailHeroSectionHeaderStyles = {
  root: "absolute inset-x-0 top-0 z-10 flex items-center justify-between px-5 pt-[max(0.875rem,env(safe-area-inset-top))]",
  actions: "flex items-center gap-2",
} as const;
