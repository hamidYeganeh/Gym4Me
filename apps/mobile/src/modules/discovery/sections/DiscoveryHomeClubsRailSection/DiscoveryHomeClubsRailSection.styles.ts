import { tv } from "tailwind-variants";

export const discoveryHomeClubsRailSectionVariants = tv({
  slots: {
    slideVertical: "!w-[min(17.5rem,78vw)]",
    slideHorizontal: "!w-[min(20rem,85vw)]",
    card: "!w-full",
  },
});
