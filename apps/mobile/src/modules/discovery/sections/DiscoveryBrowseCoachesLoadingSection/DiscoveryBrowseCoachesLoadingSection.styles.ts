import { tv } from "tailwind-variants";

export const discoveryBrowseCoachesLoadingSectionVariants = tv({
  slots: {
    stack: "flex flex-col gap-4",
    coachCardVertical: "w-[min(17.5rem,78vw)] shrink-0 snap-start",
  },
});
