import { tv } from "tailwind-variants";

export const discoveryBrowseClubsLoadingSectionVariants = tv({
  slots: {
    stack: "flex flex-col gap-4",
    clubCardVertical: "w-[min(17.5rem,78vw)] shrink-0 snap-start",
  },
});
