import { tv } from "tailwind-variants";

export const discoveryHomeClubCategoriesSectionVariants = tv({
  slots: {
    slide: "!w-[11.5rem]",
    column: "flex h-full flex-col gap-3",
    card: "!w-full !bg-surface",
  },
});
