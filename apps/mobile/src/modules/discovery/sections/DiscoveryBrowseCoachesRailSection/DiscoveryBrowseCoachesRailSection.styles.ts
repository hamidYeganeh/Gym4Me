import { tv } from "tailwind-variants";

export const discoveryBrowseCoachesRailSectionVariants = tv({
  slots: {
    slideVertical: "!w-[min(17.5rem,78vw)]",
    slideHorizontal: "!w-[min(20rem,85vw)]",
    slideFullWidth: "!w-[min(100%,calc(100vw-2.5rem))]",
    card: "!w-full",
  },
});
