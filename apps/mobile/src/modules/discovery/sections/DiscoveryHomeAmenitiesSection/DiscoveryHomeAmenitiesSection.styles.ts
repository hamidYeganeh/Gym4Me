import { tv } from "tailwind-variants";

export const discoveryHomeAmenitiesSectionVariants = tv({
  slots: {
    card:
      "h-auto w-fit min-w-[13.5rem] shrink-0 snap-start bg-transparent p-0 shadow-none",
  },
});
