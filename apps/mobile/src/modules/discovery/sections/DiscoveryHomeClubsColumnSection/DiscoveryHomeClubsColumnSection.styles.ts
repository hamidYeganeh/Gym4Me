import { tv } from "tailwind-variants";

export const discoveryHomeClubsColumnSectionVariants = tv({
  slots: {
    list: "flex w-full min-w-0 flex-col gap-3",
    card: "w-full cursor-pointer",
  },
});
