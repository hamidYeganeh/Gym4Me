import type { ReactNode } from "react";

export type OwnerHomeStaggerSectionProps = {
  children: ReactNode;
  reduceMotion?: boolean | null;
};

export type OwnerHomeStaggerItemProps = {
  children: ReactNode;
};
