import { tv } from "tailwind-variants";

export const ownerMembersImportSectionVariants = tv({
  slots: {
    root: "overflow-hidden rounded-[24px] border-0 bg-surface",
    body: "flex flex-col gap-3 p-4",
  },
});
