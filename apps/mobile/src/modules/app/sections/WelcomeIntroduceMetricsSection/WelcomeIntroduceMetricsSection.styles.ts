import { tv } from "tailwind-variants";

export const welcomeIntroduceMetricsSectionVariants = tv({
  slots: {
    root: "mx-auto mt-auto mb-2 flex w-full max-w-[22.5rem] shrink-0 flex-col gap-3",
  },
});
