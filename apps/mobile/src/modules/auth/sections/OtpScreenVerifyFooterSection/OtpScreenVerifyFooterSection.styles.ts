import { tv } from "tailwind-variants";

export const otpScreenVerifyFooterSectionVariants = tv({
  slots: {
    link: "inline-flex items-center gap-2 font-bold text-accent underline underline-offset-4 outline-none data-[hovered=true]:opacity-80",
  },
});
