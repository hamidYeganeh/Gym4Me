import { tv } from "tailwind-variants";

/** Styles live mostly inline in the migrated screen; slots reserved for follow-up tv migration. */
export const athleteWeightMetricsScreenVariants = tv({
  slots: {
    root: "",
  },
});
