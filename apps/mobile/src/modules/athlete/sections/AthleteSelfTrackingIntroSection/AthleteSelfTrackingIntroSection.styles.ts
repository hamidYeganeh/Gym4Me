import { tv } from "tailwind-variants";

export const athleteSelfTrackingIntroSectionVariants = tv({
  slots: {
    root: "flex flex-col gap-2",
    subtitle: "text-muted",
    actions: "mt-3 flex flex-wrap gap-2",
  },
});
