import { tv } from "tailwind-variants";

export const athleteSelfTrackingMetricSelectorSectionVariants = tv({
  slots: {
    root: "grid grid-cols-2 gap-2",
    metricButton: "justify-start whitespace-normal text-start",
  },
});
