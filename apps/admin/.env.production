# Loaded by Vite on `vite build` (production mode).
# Docker/CI can still override via VITE_API_BASE_URL build arg.
VITE_API_BASE_URL=https://api.gym4me.ir/api/v1
VITE_BASE_PATH=/
VITE_CLUBS_USE_MOCK=false
