# Public API URL embedded in the browser bundle.
VITE_API_BASE_URL=http://localhost:8088/api/v1
# Deployment base path; keep / when the admin uses its own host.
VITE_BASE_PATH=/
# Live admin clubs API is the default; set to "true" only for offline demos.
VITE_CLUBS_USE_MOCK=false
