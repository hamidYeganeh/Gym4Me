import { tv } from "tailwind-variants";

export const clubsListTableSectionVariants = tv({
  slots: {
    actionsCell: "flex flex-wrap items-center justify-end gap-2",
    chips: "flex flex-wrap gap-1.5",
  },
});
