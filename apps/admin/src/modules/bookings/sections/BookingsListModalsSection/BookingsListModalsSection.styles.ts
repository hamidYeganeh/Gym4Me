import { tv } from "tailwind-variants";

export const bookingsListModalsSectionVariants = tv({
  slots: { actions: "flex flex-wrap gap-2", subtitle: "max-w-2xl text-sm leading-7 text-muted sm:text-base" },
});
